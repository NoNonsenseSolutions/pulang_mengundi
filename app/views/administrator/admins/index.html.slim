h1.page-header Admins
a.btn.btn-info.active href=new_administrator_admin_path role="button" New admin
.row.placeholders
.table-responsive
  table.table.table-striped
    thead
      tr
        th #
        th Name
        th Email
        th Created at
    tbody
      - @admins.each_with_index do |admin, index|
        tr
          td= admin.id
          td= admin.name || '-'
          td= admin.email || '-'
          td= l admin.created_at
    = will_paginate @admins
